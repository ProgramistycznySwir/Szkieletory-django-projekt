import React from 'react'

const ElementDir = (props) => {

 const {id, name, content_size, parent_directory} = props.folderId;

  return (
    <div>
      
    </div>
  )
}

export default ElementDir